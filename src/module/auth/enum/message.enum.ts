export enum MessageType {
  ExistingUser = `It looks like you're already registered`,
  SuccessRegister = `Congratulations! You're registered successfully!`,
  InvalidCredential = 'Invalid Credentials. Please try again',
}
